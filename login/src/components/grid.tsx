import { IconUser } from "@tabler/icons-react"
import { Link } from "react-router-dom"

interface Props {
    open: boolean
}

function Grid(props:Props) {
    return (
        <>
            
        </>
    )
}

export default Grid